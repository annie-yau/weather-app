apiVersion: v1
kind: Service
metadata:
  name: weather-app-service
  namespace: demo-env-dev
spec:
  type: NodePort
  ports:
  - name: http
    port: 8080
    targetPort: 80
    protocol: TCP
  selector:
    app: weather-app
